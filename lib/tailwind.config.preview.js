'use strict';

module.exports = {
  presets: [require('../index')],
  darkMode: false,

  theme: {
    configViewer: {
      typographyExample: 'The quick brown fox jumped over the lazy dog.',
    },
  },
};
